## kill 杀死进程

`kill -l`：查看可用的进程信号。

`killall [选项][信号] 进程名`：按照进程名杀死进程。

`pkill [选项][信号] 进程名`：按照进程名终止进程。

`kill -1 进程ID号`：该信号让进程立即关闭，然后重新读取配置文件之后重启。

`kill -9 进程ID号`：用来立即结束程序的运行，本信号不能被阻塞、处理和忽略。一般用于强制终止进程。

`kill -15 进程ID号`：正常结束进程的信号。

如：

`pkill -9 -t pts/1`：强制杀死从 pts/1 虚拟终端登录的进程
